import React from "react";
import { useState } from "react";
import { Redirect } from "react-router-dom";
import firebaseConfig from "../config";

function SignUp() {
  cosnt[(currentUser, setCurrentUser)] = useState(null);

  return <div></div>;
}

export default SignUp;
